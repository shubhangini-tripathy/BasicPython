N = int(input())
A = [int(x) for x in input().split()]
B = []
len = 0
max_len = 0
for i in range(len(A)):
    if A[i]>0:
        if len>0 and len(B)>0:
            len=0
        b.append(A[i])
    elif len(B):
        B.pop(i)
        if B.pop(A[i])== -(A[i]):
            len += 1
        else:
            len=0
            B = []
        if len>max_len:
            max_len=len
print(max_len*2)





